class Car {
  final String name;
  final String imageUrl;

  const Car({this.name, this.imageUrl});

  // Car.formList(List<dynamic> list) {
  //   name = list[0];
  //   imageUrl = list[1];
  // }
  //
  // /// 重定向
  // Car.defaults(String name, String url):this(name: name,imageUrl: url);

}
